# Docassemble environment settings
TIMEZONE=America/New_York
POSTGRES_PASSWORD=docassemble

# SMTP settings (stubs for local development)
SMTP_SERVER=localhost
SMTP_PORT=25
SMTP_USERNAME=
SMTP_PASSWORD=
MAIL_DEFAULT_SENDER=no-reply@example.com
